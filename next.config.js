/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: Use .eslintrc.json or eslint.config.js for ESLint configuration
  // The 'eslint' field in next.config.js is deprecated in Next.js 16+
  // metadataBase: new URL('http://localhost:3002'), // Set the metadataBase property
}

module.exports = nextConfig
